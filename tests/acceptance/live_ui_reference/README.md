# Live UI Reference Acceptance Site

Phase 37I fixture site covering live DOM operation payloads, event dispatch,
region replacement, stream reconnect shape, auth expiry, backpressure, and
runtime asset serving without requiring a full browser in the default lane.
